name: paper-trading

# 24/7 в облаке: каждый час гоняем рутину, коммитим свежие данные и публикуем сайт на GitHub Pages.
on:
  schedule:
    - cron: "7 * * * *"      # каждый час в :07 UTC (GitHub может задерживать запуск на несколько минут)
  workflow_dispatch: {}        # ручной запуск кнопкой
  push:
    branches: [main]
    paths:
      - "site/**"
      - ".github/workflows/paper.yml"

permissions:
  contents: write   # коммит обновлённого состояния
  pages: write      # публикация на Pages
  id-token: write   # деплой Pages

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  run-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: pip install --quiet pandas numpy
      - name: Run paper trading
        run: python paper_trade.py
      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add site/paper.js paper_state.json
          if git diff --cached --quiet; then
            echo "Нет изменений"
          else
            git commit -m "paper: update $(date -u +%FT%TZ)"
            git push
          fi
      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - name: Deploy to Pages
        id: deploy
        continue-on-error: true   # transient GitHub Pages hiccups shouldn't fail the run; data is already committed and the next hourly run redeploys
        uses: actions/deploy-pages@v4
